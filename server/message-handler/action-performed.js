
export function handleActionPerformedMessage(message) {
    let userId = message.userId;
    let fieldState = message.fieldState;

    console.log(`[ActionPerforme] got ActionPerforme message (userId = ${userId}, fieldState = ${fieldState})`);

    
}